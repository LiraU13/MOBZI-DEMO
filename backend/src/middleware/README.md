# Middleware

Esta carpeta contiene middleware personalizado para Express.

## Estructura sugerida:

- `auth.middleware.ts` - Middleware de autenticación JWT
- `validation.middleware.ts` - Middleware de validación de datos
- `error.middleware.ts` - Manejo de errores
- `logger.middleware.ts` - Logging de requests

