# MOBZI - Backend environment example
# Copia este archivo a `.env` y añade valores reales. NO subir `.env` a git.

# Tipo de base de datos (mysql | postgresql)
DB_TYPE=mysql

# MySQL (XAMPP en desarrollo, DB gestionada en producción)
DB_MYSQL_HOST=localhost
DB_MYSQL_PORT=3306
DB_MYSQL_USER=root
DB_MYSQL_PASSWORD=
DB_MYSQL_DATABASE=mobzi
DB_MYSQL_CONNECTION_LIMIT=10

# Puerto del servidor (en plataformas PaaS la plataforma provee este valor)
PORT=3001
NODE_ENV=development
API_VERSION=v1

# CORS
# En producción pon el dominio del frontend, p.ej. https://misitio.com
CORS_ORIGIN=http://localhost:3000
CORS_CREDENTIALS=false

# JWT (cambia por valores seguros en producción)
JWT_SECRET=change-this-secret-key
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=change-this-refresh-secret
JWT_REFRESH_EXPIRES_IN=30d

# Opcional: configuraciones de logging y rate-limit
# LOG_LEVEL=info
# RATE_LIMIT_WINDOW_MS=600000
# RATE_LIMIT_MAX_REQUESTS=6000
