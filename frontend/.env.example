# MOBZI - Frontend environment example
# Copia este archivo a `.env.local` para desarrollo local o define variables en Vercel.

# URL base de la API (sin slash final). Ejemplo: https://api.misitio.com/api/v1
NEXT_PUBLIC_API_BASE=http://localhost:3001/api/v1

# Mapbox (si lo usas) - añade tu clave en producción
NEXT_PUBLIC_MAPBOX_TOKEN=
